##字符串拼接
#set( size = "Big" )
#set( name = "Ben" )
The clock is           ${size+name}.

#set(clock = size+" Tall " + name )
The clock is          ${clock}.

${fmt("The clock is %s %s",size,name)}


##字符串length长度计算
${"abcd".length()}