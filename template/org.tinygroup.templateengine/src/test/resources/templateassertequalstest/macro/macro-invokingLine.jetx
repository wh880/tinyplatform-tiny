## 示例1：
  #macro header(subTitle)
    <i>Tiny框架：${subTitle}</i>
  #end
  
## 调用方式：
  #header()
  #header("abc")
  
 ***************************************************
 ## 示例2：
      #macro hello(a,b,c,d,e)
         a=${a}
         b=${b}
         c=${c}
         d=${d}
         e=${e}
     #end
     各种调用方式：
     #hello()          【表示调用#hello的时候,值未设定】
     #hello(1,2,3,4,5) 【表示调用#hello的时候，a=1,b=2,c=3,d=4,e=5】
     #hello(1,2)       【表示调用#hello的时候，a=1,b=2其它值未设定】
     #hello(b=2,1)     【表示调用hello33的时候，b先等于2，然后b又被覆盖，最后b=1其他值未设定】
     #hello(e=5,2,3)   【表示调用#hello的时候，b=2,c=3,e=5其它值未设定】
     #hello(e=5,d=4)   【表示调用#hello的时候，d=4,e=5其它值未设定】
     #hello(e=5)       【表示调用#hello的时候，e=5 ,其他值未设定】
   
     
 ***************************************************
 ## 示例3： call函数方式   无包含内容调用

#call("hello")
#call("hello",1,2)