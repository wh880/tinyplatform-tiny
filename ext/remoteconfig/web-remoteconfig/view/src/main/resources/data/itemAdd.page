#@tForm("thisform" call("link","/remoteconfig/node/addItem?id="+id) "post" {"class":""})
	 #@elementItem("key：")
        #tInput("key" "" {"placeholder":"key","datatype":"*1-16","nullmsg":"key不能为空！","id":"key","ajaxurl":call("link","/remoteconfig/node/checkNode?id="+id)})
    #end

    #@elementItem("value：")
        #tInput("value" "" {"placeholder":"value"})
    #end
	
#end
<script>
 $(function(){
        $("form[name=thisform]").Validform({
            beforeSubmit: function (curform) {
                $(curform).ajaxSubmit({
                    dataType: "json",
                    success: function (data) {
                        if (data.status == "y" || data.status == "success") {
                            layer.msg(data.info, {icon:1}, function () {
                                $(curform).closest(".modal").trigger("close");
                            	$("#show-content").load('#link("/remoteconfig/node/getAllItem?id="+id)');
                            });
                        } else {
                            layer.msg(data.info,{icon:2});
                        }
                    },
                    error: function () {
                        layer.msg("error",{icon:2});
                    }
                });
                return false;
            }
        });
    });
</script>