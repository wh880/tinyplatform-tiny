#for ( x : ["a","b","c","d"])
    ${xFor.index} - ${xFor.size}: ${x}
    ${xFor.atFirst} - ${xFor.atLast}
#end
#for (x : {})
    ${xFor.index} - ${xFor.size}: ${x}
    ${xFor.atFirst} - ${xFor.atLast}
#end