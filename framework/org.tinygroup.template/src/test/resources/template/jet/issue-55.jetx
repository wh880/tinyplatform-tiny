#for ( x : ["a","b","c","d"])
    ${xFor.index} - ${xFor.size}: ${x}
    ${xFor.isFirst} - ${xFor.isLast}
#end
#for (x : {})
    ${xFor.index} - ${xFor.size}: ${x}
    ${xFor.first} - ${xFor.last}
#end